# Assignment-Syncing-State-with-LocalStorage-using-a-Custom-Hook
# React useLocalStorage Lab

This project demonstrates a custom React hook called useLocalStorage.

## Features

- Saves Dark Mode preference
- Saves user's name
- Persists data after page refresh
- Uses React Hooks and localStorage

## Installation

npm install

## Run

npm run dev
